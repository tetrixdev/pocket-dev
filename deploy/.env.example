# PocketDev Configuration (Production)
# Run ./setup.sh to auto-populate these values, or copy to .env and fill manually

# Application URL
# Update to your Tailscale URL for remote access
APP_URL=http://localhost

# Force HTTPS for all generated URLs
# Enable when using Tailscale Serve or another reverse proxy that terminates SSL
FORCE_HTTPS=false

# Laravel Application Key (auto-generated by setup.sh)
APP_KEY=

# Database passwords (auto-generated by setup.sh)
DB_PASSWORD=

# Read-only database user password (auto-generated by setup.sh)
# Used by MemoryQueryTool for secure read-only access
DB_READONLY_PASSWORD=

# Memory AI database user password (auto-generated by setup.sh)
# Used by memory:schema:* tools for DDL/DML operations on memory schema
DB_MEMORY_AI_PASSWORD=

# HTTP port for PocketDev (default: 80)
NGINX_PORT=80

# Docker socket group ID (auto-detected by setup.sh)
# Required for Docker operations from within PocketDev
DOCKER_GID=

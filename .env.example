# PocketDev Configuration
# Run ./setup.sh to auto-populate these values, or copy to .env and fill manually

# Application settings
APP_NAME=pocket-dev
APP_ENV=local
APP_DEBUG=true

# This should be updated to Tailscale URL if deployed not locally!
APP_URL=http://localhost

# Force HTTPS for all generated URLs
# Enable when using Tailscale Serve or another reverse proxy that terminates SSL
# Not needed for local network access (192.168.x.x) - browsers treat those as secure
FORCE_HTTPS=false

# Laravel Application Key (auto-generated by setup.sh)
APP_KEY=

# Database values
DB_CONNECTION=pgsql
DB_HOST=pocket-dev-postgres
DB_DATABASE=pocket-dev
DB_USERNAME=pocket-dev

# Database password (auto-generated by setup.sh)
DB_PASSWORD=

# Read-only database user password (auto-generated by setup.sh)
# Used by MemoryQueryTool for secure read-only access
DB_READONLY_PASSWORD=

# Memory AI database user password (auto-generated by setup.sh)
# Used by memory:schema:* tools for DDL/DML operations on memory schema
DB_MEMORY_AI_PASSWORD=

# Redis configuration
REDIS_CLIENT=predis
REDIS_HOST=pocket-dev-redis
REDIS_PASSWORD=null

# Queue and cache
QUEUE_CONNECTION=redis
CACHE_STORE=redis

# HTTP port for PocketDev (default: 80)
NGINX_PORT=80

# File permissions - must match your host user
# Auto-detected by setup.sh, or run: id -u (USER_ID) and id -g (GROUP_ID)
USER_ID=1000
GROUP_ID=1000

# Docker socket group ID (auto-detected by setup.sh)
# Required for Docker operations from within PocketDev
# Run: stat -c '%g' /var/run/docker.sock
# DOCKER_GID=

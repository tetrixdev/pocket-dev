services:
  # Main nginx reverse proxy - handles both Laravel and TTYD
  pocket-dev-proxy:
    image: nginx:alpine
    container_name: pocket-dev-proxy
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - pocket-dev-nginx
      - pocket-dev-ttyd
    networks:
      - pocket-dev
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  pocket-dev-php:
    build:
      context: .
      dockerfile: docker-laravel/local/php/Dockerfile
    container_name: pocket-dev-php
    restart: unless-stopped
    working_dir: /var/www
    entrypoint: ["/entrypoint.sh"]
    ports:
      - "${VITE_PORT:-5173}:5173"
    volumes:
      - ./www:/var/www
      - .env:/var/www/.env
      - ./docker-laravel/shared/php/local.ini:/usr/local/etc/php/conf.d/local.ini
      - ./docker-laravel/local/php/entrypoint.sh:/entrypoint.sh
    networks:
      - pocket-dev
    depends_on:
      pocket-dev-postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "php-fpm -t || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 3
      start_period: 20s

  pocket-dev-nginx:
    image: nginx:1.29-alpine
    container_name: pocket-dev-nginx
    restart: unless-stopped
    volumes:
      - ./www:/var/www
      - ./docker-laravel/shared/nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - pocket-dev
    depends_on:
      pocket-dev-php:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  pocket-dev-postgres:
    image: postgres:17-alpine
    container_name: pocket-dev-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: pocket-dev
      POSTGRES_USER: pocket-dev
      POSTGRES_PASSWORD: "${DB_PASSWORD:-laravel}"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      - "${DB_PORT:-5432}:5432"
    networks:
      - pocket-dev
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pocket-dev -d pocket-dev"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  pocket-dev-ttyd:
    build:
      context: .
      dockerfile: docker-ttyd/Dockerfile
    container_name: pocket-dev-ttyd
    restart: unless-stopped
    working_dir: /workspace
    user: "1001:1001"
    group_add:
      - "1000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./workspace:/workspace
      - ./docker-ttyd/entrypoint.sh:/entrypoint.sh
    networks:
      - pocket-dev
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:7681/ || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 3
      start_period: 20s

networks:
  pocket-dev:
    driver: bridge

volumes:
  postgres-data:
    driver: local
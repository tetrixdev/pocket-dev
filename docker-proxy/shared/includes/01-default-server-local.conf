# =============================================================================
# LOCAL MODE - No IP Blocking
# =============================================================================
# In local development mode, we don't need IP blocking. The main server block
# has the default_server flag and accepts all requests regardless of Host header.
# =============================================================================
